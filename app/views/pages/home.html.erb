<h1 style="text-align: center">Welcome to the Bunkaipedia</h1>
<p>
  This is an interactive site dedicated to the study of kata in the martial arts.  
</p>
<% unless user_signed_in? %>
  <div class="signup">
    <%= link_to "Sign up", new_user_registration_path, :class => 'buttonlike round' %>
  </div>
<% end %>
<h3>See the kata for:</h3>
<div class="styles">
  <ul class="flat">
    <% @styles.each do |style| %>
      <li><%= link_to style.name, style_path(style) %></li>
    <% end %>
  </ul>
</div>
<div>
  <span style="float:right">
    <%= render 'shared/show_hide', :show_hide_div => 'recent_bunkai', 
      :switch_text_div => 'bunkai_show_hide' %>
  </span>
  <h3>Recent bunkai:</h3>
</div>
<ul class="bunkai" id="recent_bunkai" style="display:block">
  <%= render @bunkai %>
<ul>