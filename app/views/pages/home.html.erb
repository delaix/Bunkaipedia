<h1 style="text-align: center">Welcome to the Bunkaipedia</h1>
<p>
  This is an interactive site dedicated to the study of kata in the martial arts.  
</p>
<% unless user_signed_in? %>
  <div class="signup">
    <%= link_to "Sign up", new_user_registration_path, :class => 'buttonlike round' %>
  </div>
<% end %>
<p>
  See the kata for:
</p>
 <div class="styles">
   <ul class="flat">
     <% @styles.each do |style| %>
       <li><%= link_to style.name, style_path(style) %></li>
     <% end %>
   </ul>
 </div>
